"""Runs on any Python >= 3.10, provisions a CPython 3.12.10 environment holding
`mship-engine`, and execs into it."""

# Lockstep with mship-engine; `make _release` bumps both pyprojects.
__version__ = "0.7.15"
