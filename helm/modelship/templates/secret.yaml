{{- if not .Values.secrets.existingSecret }}
{{- $inlineRedisPw := and .Values.redis.password (not .Values.redis.existingSecret) }}
{{- if or .Values.secrets.huggingfaceToken .Values.secrets.apiKeys $inlineRedisPw }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "modelship.secretName" . }}
  labels:
    {{- include "modelship.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- if .Values.secrets.huggingfaceToken }}
  HF_TOKEN: {{ .Values.secrets.huggingfaceToken | quote }}
  {{- end }}
  {{- if .Values.secrets.apiKeys }}
  MSHIP_API_KEYS: {{ .Values.secrets.apiKeys | quote }}
  {{- end }}
  {{- if $inlineRedisPw }}
  {{ .Values.redis.passwordKey }}: {{ .Values.redis.password | quote }}
  {{- end }}
{{- end }}
{{- end }}
{{- if and .Values.rayAuth.enabled .Values.rayAuth.token (not .Values.rayAuth.existingSecret) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "modelship.rayAuthSecretName" . }}
  labels:
    {{- include "modelship.labels" . | nindent 4 }}
type: Opaque
stringData:
  auth_token: {{ .Values.rayAuth.token | quote }}
{{- end }}
